# Commit checkpoint — vault-infra

Run from `/Users/bartolli/llm-wiki/vault-infra/`:

```bash
git commit -S -m "$(cat <<'EOF'
fix(sync): wikilink targets preserve their existing extension

- regex check for any extension on the target; only bare targets get `.md` appended
- embeds like `![[scope-dashboard.base]]` no longer produce bogus `links.target_path` of `scope-dashboard.base.md`
EOF
)"
```

## Staged

```
A  local_docs/commits-log/2026-04-25-14-09.md
A  local_docs/commits-log/2026-04-25-14-19.md
M  packages/sync/src/sync.ts
```

## Verification

Before fix: `links.target_path = scope-dashboard.base.md`
After fix:  `links.target_path = scope-dashboard.base`
