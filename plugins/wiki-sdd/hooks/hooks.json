{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "node",
            "args": ["${CLAUDE_PLUGIN_ROOT}/hooks/wiki-frontmatter-check.mjs", "${user_config.vault_path}"]
          }
        ]
      }
    ]
  }
}
