{
  "mcpServers": {
    "wiki": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@bartolli/kmd@latest", "mcp", "${user_config.vault_path}"],
      "env": {
        "WIKI_VAULT": "${user_config.vault_path}",
        "LOG_LEVEL": "info"
      }
    }
  }
}
