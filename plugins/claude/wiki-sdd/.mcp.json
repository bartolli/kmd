{
  "mcpServers": {
    "wiki": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@bartolli/kmd@latest", "mcp", "--default-root", "${user_config.vault_path}"],
      "env": {
        "KMD_PROJECT_DIR": "${CLAUDE_PROJECT_DIR}",
        "LOG_LEVEL": "info"
      }
    }
  }
}
