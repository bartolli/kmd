{
  "name": "@llm-wiki/mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "wiki-mcp stdio server: two read-only tools \u2014 prime and search.",
  "bin": {
    "wiki-mcp": "./bin/stdio.ts"
  },
  "exports": {
    "./start": "./src/start.ts",
    "./local": "./src/local.ts"
  },
  "scripts": {
    "dev": "tsx watch bin/stdio.ts",
    "start": "tsx bin/stdio.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@llm-wiki/db": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "pino": "^10.3.0",
    "yaml": "^2.9.0",
    "zod": "^4.0.0"
  }
}
